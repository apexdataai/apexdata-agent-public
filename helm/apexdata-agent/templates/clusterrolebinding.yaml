{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "apexdata-agent.fullname" . }}-binding
  labels:
    {{- include "apexdata-agent.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "apexdata-agent.fullname" . }}-viewer
subjects:
  - kind: ServiceAccount
    name: {{ include "apexdata-agent.serviceAccountName" . }}
    namespace: {{ include "apexdata-agent.namespace" . }}
{{- end }}
